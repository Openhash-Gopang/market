# 모든 텍스트 파일: 저장소는 LF, 체크아웃은 OS 기본값
* text=auto

# 웹 파일은 LF 고정 (Windows에서도 LF 유지)
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.json text eol=lf
*.md   text eol=lf
*.sql  text eol=lf
CNAME  text eol=lf

# 바이너리는 변환 안 함
*.png  binary
*.jpg  binary
*.ico  binary
